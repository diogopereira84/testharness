var config = {
    map: {
        '*': {
            dateRangePicker: 'Fedex_Orderhistory/js/daterangepicker'
        }
    },
    shim: {
      'dateRangePicker': {
          deps: ['jquery',',momentjs']
      }
    }
}
